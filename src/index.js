import React from 'react';
import ReactDOM from 'react-dom';
import App from './App'; // You will need to create this component

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
